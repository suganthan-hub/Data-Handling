barplot(Sales,
        names.arg = Month,
        col = c("red", "blue", "green", "orange", "purple"),
        xlab = "Month",
        ylab = "Sales (in $)",
        main = "Monthly Sales Data")

